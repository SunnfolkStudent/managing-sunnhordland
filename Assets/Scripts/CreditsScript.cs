using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CreditsScript : MonoBehaviour
{
    public Button exitCreditsButton;
    
    // Update is called once per frame
    void Update()
    {
        if (exitCreditsButton)
        {
            
        }
    }
}
